{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "main-no-system-access",
	"description": "Main webview has no Tauri IPC permissions. Networking uses browser fetch only.",
	"windows": ["main"],
	"permissions": []
}
